## Packages
react-leaflet | Map visualization for PFAS Radar
leaflet | Core map library
recharts | Data visualization for water quality charts
framer-motion | Smooth animations and transitions
clsx | Class name utility (if not already present)
tailwind-merge | Tailwind class merging (if not already present)
lucide-react | Icons (already in base, but confirming)
date-fns | Date formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit'", "sans-serif"],
  body: ["'DM Sans'", "sans-serif"],
}
